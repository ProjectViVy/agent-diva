//! LLM-facing context boundary markers.

pub(crate) const CANONICAL_CHECKPOINT_HINT: &str =
    "The canonical checkpoint is bounded replacement state; the durable transcript remains the audit source.";
