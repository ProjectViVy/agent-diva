Fix sandbox type compatibility between core schema and sandbox crate.

FIXES NEEDED:

## Fix 1: agent-diva-core/src/config/schema.rs
Find the existing SandboxConfig struct. Make these changes:
1. Add `deny_patterns: Vec<String>` field with `#[serde(default)]`
2. Rename `timeout` field to `timeout_seconds` (keep `#[serde(default = "default_sandbox_timeout")]` pointing to same function)
3. If the function is named `default_sandbox_timeout`, keep it (rename if it references `timeout`)
4. Add `deny_patterns` default in the `Default` impl as `Vec::new()`

## Fix 2: agent-diva-sandbox/src/manager.rs
Find struct SandboxConfig (around line 107-123). Make these changes:
1. Add `Serialize, Deserialize` to the derive macro
2. Add `PartialEq` to the derive macro
3. Add `deny_patterns: Vec<String>` field with `#[serde(default)]`
4. Rename `default_timeout` field to `timeout_seconds` — use `#[serde(alias = "default_timeout")]` for backward compat
5. Add `deny_patterns: Vec::new()` in Default impl
6. Update `from_core_config()` method to map `core_config.timeout_seconds` and `core_config.deny_patterns.clone()`
7. Update any self-references from `self.default_timeout` to `self.timeout_seconds`

## Fix 3: agent-diva-sandbox/src/policy.rs
Add `Serialize, Deserialize` to these enums (if not already):
- SandboxMode (currently has `#[serde(rename_all = "kebab-case")]` — keep it)
- AskForApproval
- WindowsSandboxLevel

After all fixes, run: cargo build -p agent-diva-core -p agent-diva-sandbox
Fix any remaining errors.
