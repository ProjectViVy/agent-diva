审查 agent-diva-gui Tauri 后端对 sandbox 的支持情况。

审查范围：
1. agent-diva-gui/src-tauri/src/commands.rs — 所有 Tauri 命令
2. agent-diva-gui/src-tauri/src/lib.rs — Tauri 应用设置
3. agent-diva-gui/src-tauri/Cargo.toml — 依赖（是否包含 agent-diva-sandbox）
4. agent-diva-gui/src/api/desktop.ts — 前端 API 类型（是否有 sandbox 类型）
5. agent-diva-gui/src/components/settings/SandboxSettingsSection.vue — GUI 调用的命令

审查维度：
- 缺失命令：GUI invoke('get_sandbox_config') 和 invoke('save_sandbox_config') 是否有 Tauri #[command] 实现？
- 依赖链：agent-diva-gui Cargo.toml 是否依赖 agent-diva-sandbox？
- 类型桥梁：是否需要实现 Serde 适配层将 sandbox 类型转为 GUI-friendly 格式？
- 配置存储：sandbox 配置应该存在哪里？（agent-diva 的 config 系统？独立文件？）
- SelfEvolution 集成：SelfEvolutionSettings.vue 是否有 sandbox 相关逻辑？

输出中文报告，格式：
## 发现 (severity: critical|high|medium|low)
- 文件:行号 — 问题描述 → 修复建议
