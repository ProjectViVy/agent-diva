审查 agent-diva-sandbox crate 对 agent-diva-pro GUI 的适配性。

审查范围：
1. agent-diva-sandbox/src/lib.rs — 公共 API 导出
2. agent-diva-sandbox/src/manager.rs — SandboxManager, SandboxConfig, SandboxExecRequest
3. agent-diva-sandbox/src/policy.rs — SandboxMode, AskForApproval, WindowsSandboxLevel, SandboxPolicy
4. agent-diva-sandbox/src/filesystem.rs — FileSystemSandboxPolicy
5. agent-diva-sandbox/src/approval.rs — ApprovalStore, ExecApprovalRequirement

GUI 侧需求（参考 agent-diva-gui/src/components/settings/SandboxSettingsSection.vue）：
- invoke('get_sandbox_config') → 返回 { mode, approval_policy, network_access, writable_roots, protected_paths, deny_patterns, timeout_seconds }
- invoke('save_sandbox_config', { config }) → 保存配置

审查维度：
- API 兼容性：sandbox crate 的类型是否与 GUI 期望的 SandboxConfig interface 匹配？
- 缺失功能：GUI 需要的字段（network_access, writable_roots, protected_paths, deny_patterns, timeout_seconds）在 sandbox crate 中是否有对应？
- 序列化：sandbox 类型是否实现了 Serialize/Deserialize？
- 配置持久化：sandbox crate 是否有配置加载/保存机制？还是需要 Tauri 命令层自己实现？

输出中文报告，格式：
## 发现 (severity: critical|high|medium|low)
- 文件:行号 — 问题描述 → 修复建议
