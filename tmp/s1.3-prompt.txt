Story 1.3: Merge agent_loop.rs — the most critical conflict. Two sets of changes must coexist.

Working directory: C:/Users/Administrator/Desktop/morediva/agent-diva-pro
Branch: merge-vrm-into-pro (merge in progress, conflicted files exist)

## Background
- Merge-base: 50f58c2
- VRM side: added ~1970 lines (mentle tool assembly), removed ~55 lines
- PRO side: 7 commits integrating sandbox hooks
- BOTH modify agent-diva-agent/src/agent_loop/agent_loop.rs

## Acceptance Criteria (ADR-6):
1. Sandbox hooks code MUST remain intact (NO feature gate)
2. Mentle tool assembly MUST be in #[cfg(feature = "mentle")] blocks
3. Order: sandbox registers first, mentle appends after
4. VRM files must exist: mentle_runtime.rs, mentle_discovery.rs, tool_config/mentle.rs
5. cargo check -p agent-diva-agent must pass
6. cargo check -p agent-diva-agent --features mentle must pass

## Approach:
1. Read conflicted agent_loop.rs first
2. Read VRM's files: mentle_runtime.rs, mentle_discovery.rs, tool_config/mentle.rs to understand what mentle adds
3. 3-way merge: keep pro's sandbox code as-is, wrap vrm's mentle code in #[cfg(feature = "mentle")]
4. Handle ALL merge conflict markers (<<<<<<<, =======, >>>>>>>)
5. Run cargo check -p agent-diva-agent to verify
6. Run cargo check -p agent-diva-agent --features mentle to verify

CRITICAL: Do NOT accidentally delete sandbox code. Do NOT let mentle code leak outside feature gate.