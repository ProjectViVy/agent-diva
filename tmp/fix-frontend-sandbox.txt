Fix sandbox frontend issues.

FIXES NEEDED:

## Fix 1: agent-diva-gui/src/api/desktop.ts
Add SandboxConfig type and API functions at the end of the file, following the same pattern as existing API functions (e.g., getConfigStatus, getProviders):

```typescript
export interface SandboxConfig {
  mode: 'danger-full-access' | 'read-only' | 'workspace-write'
  approval_policy: 'never' | 'on-failure' | 'on-request' | 'unless-trusted'
  network_access: boolean
  writable_roots: string[]
  protected_paths: string[]
  deny_patterns: string[]
  timeout_seconds: number
}

export async function getSandboxConfig(): Promise<SandboxConfig> {
  return invoke<SandboxConfig>('get_sandbox_config')
}

export async function saveSandboxConfig(config: SandboxConfig): Promise<void> {
  return invoke('save_sandbox_config', { config })
}
```

IMPORTANT: The enum values use kebab-case (matching the Rust serde output from `#[serde(rename_all = "kebab-case")]`). The OLD Vue code uses PascalCase — the Vue component ALSO needs updating (see Fix 2).

## Fix 2: agent-diva-gui/src/components/settings/SandboxSettingsSection.vue
1. Replace the inline `interface SandboxConfig` with an import from `../../api/desktop`:
   `import { getSandboxConfig, saveSandboxConfig, type SandboxConfig } from '../../api/desktop'`

2. Update SANDBOX_MODES to use kebab-case:
   `const SANDBOX_MODES: SandboxConfig['mode'][] = ['danger-full-access', 'read-only', 'workspace-write']`

3. Update APPROVAL_POLICIES to use kebab-case:
   `const APPROVAL_POLICIES: SandboxConfig['approval_policy'][] = ['never', 'on-failure', 'on-request', 'unless-trusted']`

4. Fix the textarea v-model bug on line ~311: change `v-model="config.deny_patterns"` to `v-model="denyPatternsText"`. The `denyPatternsText` computed already exists and correctly converts between array and newline-separated string.

5. Replace `invoke<SandboxConfig>('get_sandbox_config')` with `getSandboxConfig()`

6. Replace `invoke('save_sandbox_config', { config: { ...config.value } })` with `saveSandboxConfig({ ...config.value })`

DO NOT change any styles or template structure — only the script section.

After fixing, verify: search for any remaining `invoke('get_sandbox` or `invoke('save_sandbox` calls — none should remain.
