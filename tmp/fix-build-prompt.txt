Fix compilation errors after merging agent-diva-sandbox into agent-diva-pro. The merge used sandbox's backend code as base. All compilation errors below need to be fixed. Make minimal changes - preserve sandbox's intent while making code compile.

CRITICAL RULES:
- Run `cargo build --all 2>&1` to check progress after each batch of fixes
- Make minimal targeted edits, do NOT rewrite entire files
- Fix errors in this order: agent-diva-tools first (missing modules), then agent-diva-sandbox (missing config types), then agent-diva-providers (type mismatch)
- After all fixes pass, run `cargo build --all` one final time

=== ERROR GROUP 1: agent-diva-tools ===
Files: agent-diva-tools/src/lib.rs, agent-diva-tools/src/base.rs, agent-diva-tools/src/registry.rs, agent-diva-tools/src/filesystem.rs

Problem: lib.rs imports modules `base` and `registry` but these files don't exist in sandbox. Also references `agent_diva_tooling` which isn't a dependency. Sandbox's tools don't use these.

Fix approach: Read agent-diva-tools/src/lib.rs and agent-diva-tools/src/filesystem.rs. Remove references to `mod base`, `mod registry`, and `agent_diva_tooling`. Check sandbox's original tools/lib.rs from: `git show sandbox/agent-diva-with-sandbox:agent-diva-tools/src/lib.rs`

=== ERROR GROUP 2: agent-diva-sandbox ===
Files: agent-diva-sandbox/src/*.rs

Problem: agent-diva-sandbox uses `SandboxConfig`, `AskForApproval`, `SandboxMode`, `WindowsSandboxLevel` from `agent_diva_core::config` but these don't exist in sandbox's core.

Fix approach: Read agent-diva-sandbox/src/*.rs to understand how these types are used. Then either: (a) define minimal stub types in agent-diva-core/src/config/, or (b) add them to agent-diva-sandbox's own types. Prefer (a) so sandbox crate stays clean.

=== ERROR GROUP 3: agent-diva-providers ===
File: agent-diva-providers/src/ollama.rs

Problem: `msg.content.clone()` returns `MessageContent` but `content:` field expects `String`. lines 162, 171, 178.

Fix approach: Check what type `Message::content` is in agent-diva-core. If it's `MessageContent` (complex enum), convert to String. Look for a `.to_string()` or similar method on `MessageContent`.

=== ERROR GROUP 4: uuid ===
Problem: Some file uses `uuid` but it's not in dependencies.
Read the file referencing uuid and either add uuid to Cargo.toml or remove the usage.
