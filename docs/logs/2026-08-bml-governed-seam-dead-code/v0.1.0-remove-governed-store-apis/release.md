# Release

Not a product release. Land on `chore/bml-governed-seam-dead-code` and
merge to `dev` when the parallel workspace-agents / GUI style-phase2
lanes are ready.

Merge caution: `TODOLIST.md` and `LOCK.md` may conflict with those
worktrees; resolve manually. Production files in this change are
`agent-diva-laputa` only plus the AGENTS write-API sentence.

Do not push unless explicitly requested.
