# Acceptance

## 用户/维护者验收

1. 打开根 `TODOLIST.md`，确认 Workspace 完成 WBS 不再作为活跃开发项出现。
2. 在 Archive Index 中确认存在 `completed-2026-08-28-workspace-system-closeout.md`。
3. 打开归档正文，确认 WS-00～WS-06、旧 Workspace 条目、验收结论和关闭边界均有记录。
4. 确认 CLI legacy 默认值兼容策略仍作为独立后续项保留。
5. 确认 `dev` 是当前唯一 Workspace 开发主线，旧分支/worktree 不再作为入口。
