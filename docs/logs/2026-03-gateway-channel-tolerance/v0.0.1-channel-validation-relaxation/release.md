# Release

- No special deployment steps are required beyond shipping the updated binaries.
- Existing user configs are preserved. Invalid enabled channels will now be skipped at runtime instead of blocking startup.
