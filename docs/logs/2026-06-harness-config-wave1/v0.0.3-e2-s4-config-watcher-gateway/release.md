# Release

- No separate deployment step was executed.
- This iteration changes Rust workspace runtime wiring and was validated locally through targeted tests, `cargo check`, and real gateway `/api/health` smoke paths.
