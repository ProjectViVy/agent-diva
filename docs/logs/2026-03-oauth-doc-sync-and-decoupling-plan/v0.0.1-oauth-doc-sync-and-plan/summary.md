# Summary

- 同步了 `agent-diva` 当前 provider OAuth 文档口径，移除了 `provider login` 仍是 placeholder 的过期描述。
- 更新了用户文档中的 `openai-codex` 登录示例，补充 `use` / `refresh` / `logout` 等已落地命令。
- 新增一份 `agent-diva` OAuth 解耦实施计划，说明与 `zeroclaw` 的结构差异、当前耦合点和建议实施顺序。
