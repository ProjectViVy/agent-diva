# Acceptance

1. `docs/user-guide/commands.md` 不再把 `provider login` 描述为 placeholder。
2. `docs/userguide.md` 不再声称 `provider login` 返回 `not_implemented`。
3. 用户文档中出现 `openai-codex` 的真实登录示例，而不是旧的 `openai` 示例。
4. 新的 OAuth 解耦计划文档明确写出当前耦合点、`zeroclaw` 的四层结构和分阶段实施顺序。
