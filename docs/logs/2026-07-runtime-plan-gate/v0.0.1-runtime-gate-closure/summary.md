# Runtime Plan Gate Closure

P3 now derives one effective tool phase for turn assembly, invocation, and rebuilds. Terminal plans no longer lock ordinary conversations, explicit plan-mode remains restrictive, and runtime approval/configuration updates rebuild the phase-filtered registry.

The approval transport now carries both the refreshed runtime plan and its persisted revision-bound approval receipt, providing the backend contract for P4 without changing GUI behavior.
