# 用户验收

1. 启动 Agent Diva 桌面端并进入主页。
2. 让一个计划进入待审批状态，打开主页审批中心。
3. 点击该计划的“一次性允许”。
4. 预期：审批卡片从待审批状态消失，源会话中的计划立即进入执行态并开始续接执行，不需要再次点击聊天页的旧批准按钮。
5. 若执行流首次启动失败，预期：审批抽屉关闭，源会话计划面板显示错误信息和“继续执行”按钮；点击后使用同一计划执行上下文重试，不重复创建审批。
6. 其他审批域的允许、拒绝和取消行为应保持原有结果。
