# Pending plan history actions

The plan-history detail now exposes the existing approval controls when the selected plan is the active `AwaitingApproval` plan. Historical, executing, and terminal reports remain read-only.
