# Verification

- `pnpm exec vue-tsc --noEmit` -- passed.
- `pnpm build` -- passed (type check and production Vite build).
- `git diff --check` -- passed.

The production build provides the GUI smoke validation for this static dashboard-order change.
