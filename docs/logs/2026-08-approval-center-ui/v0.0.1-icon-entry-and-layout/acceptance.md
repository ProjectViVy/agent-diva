# Acceptance

1. Chat page shows history icon at top-right; Approval Center is a second icon directly below it (not a floating top badge with full title text).
2. Pending count badge on the Approval icon matches global pending approvals.
3. Clicking the icon opens the right-side drawer; Esc / backdrop / close dismisses it.
4. Drawer header, filters, and card list/empty state are non-overlapping and readable.
5. Inline chat approval cards still work; legacy Command banner remains suppressed when unified Command approval is present.
