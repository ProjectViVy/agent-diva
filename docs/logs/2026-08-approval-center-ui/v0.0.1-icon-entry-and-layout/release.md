# Release

Source-only GUI layout update. No package publish, no push, no installer rebuild required for acceptance of this slice.

Rollback: revert the focused commit for this version directory; legacy fixed floating trigger would return and the chat-corner icon stack would disappear.
