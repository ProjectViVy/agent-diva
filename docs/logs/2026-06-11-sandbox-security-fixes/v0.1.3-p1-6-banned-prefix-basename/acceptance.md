# Acceptance

1. Evaluate `is_banned_prefix()` with `python3 -c` and `/usr/bin/python3 -c`.
2. Confirm both variants are treated as banned prefixes.
3. Confirm unrelated absolute-path commands such as `/usr/bin/git status` remain allowed.
