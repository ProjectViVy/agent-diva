# Acceptance

1. 在 GUI 或 API 聊天入口发送“文本 + 图片附件”。
2. 当当前模型为 `gpt-4o` 或 `gpt-4.1` 这类已知视觉模型时，模型应能直接基于图片内容回复，而不是只看到附件占位提示。
3. 当当前模型为不支持视觉的模型时，请求应直接失败，并返回明确的“当前模型不支持 vision input，请切换视觉模型”提示。
4. 纯文本附件行为不应回归：小文本仍可内联，非文本/过大附件仍保持原有工具提示策略。
