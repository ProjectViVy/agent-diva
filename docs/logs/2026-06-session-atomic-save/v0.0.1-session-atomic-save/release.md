# Release

Release method: normal workspace build and release flow.

No migration is required. Session storage paths and JSONL schema are unchanged; only the write mechanism changed from direct final-path write to temporary-file replacement.
