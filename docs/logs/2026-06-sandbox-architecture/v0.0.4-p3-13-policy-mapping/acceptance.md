# Acceptance

1. `SandboxPolicy` can be mapped into `SecurityPolicy`.
2. `SecurityPolicy` can be mapped back into `SandboxPolicy` through the exported extension trait.
3. Conversion behavior is documented and covered by tests.
