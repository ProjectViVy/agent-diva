# Acceptance

1. Send a response containing only a short reasoning trace and confirm both copy and expand controls remain distinct and clickable.
2. Send short user and assistant messages and confirm the bubble has a readable minimum width without forcing an early line break.
3. Send an unbroken long token and confirm it wraps inside the bubble instead of causing horizontal overflow.
