# Acceptance

## Checks

- Open the animation panel.
- Confirm `Relax`, `Sleepy`, and `LookAround` appear under one-shot preview rather than idle motions.
- Open the appearance editor.
- Confirm `show_full_body` appears as a startup motion choice along with `appearing` and `greeting`.
- Save an appearance with `show_full_body` and switch to it; the avatar should play the full-body startup motion.
