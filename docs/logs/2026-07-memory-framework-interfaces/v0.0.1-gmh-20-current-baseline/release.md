# GMH-20 Release

This is an architecture/specification release only. It needs no deployment,
feature flag, schema migration, data import, configuration change, Manager
route, Tauri command, or GUI release.

Rollback is a normal revert of the focused documentation commit. Existing
Memory runtime and storage behavior remains unchanged either way.
