# Release method

Build the desktop bundle from `agent-diva-gui` with `npm run tauri build`, then start the generated executable with an isolated profile for acceptance. This iteration's release executable was rebuilt; the existing NSIS installer was not deployed.

No installer was deployed and no remote branch was pushed. Persistent evolution changes remain review-gated proposals; this repair does not relax that boundary.
