# Acceptance

- 选择 Windows `Pictures` 目录后，入口名称显示 `Pictures`。
- 展开工作区入口时，路径显示 `C:\Users\Administrator\Pictures`，不显示 `\\?\`。
- 工作区设置页的当前路径、候选 canonical root 和 AGENTS.md 来源路径采用相同规则。
- 工作区切换仍提交原始 canonical root，切换和 session/workspace 归属不受展示格式影响。
