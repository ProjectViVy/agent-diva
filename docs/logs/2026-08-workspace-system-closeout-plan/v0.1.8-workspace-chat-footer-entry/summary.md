# Workspace 聊天底栏入口

## 变更

- 根据真机验收反馈，移除 Topbar 中不易发现的 `WorkspaceChip`。
- 将唯一工作区入口放到聊天输入框底栏，紧邻上下文预算右侧。
- 复用现有 workspace snapshot、刷新和 Settings 原子切换流程，没有新增第二套状态。
- 底部入口的 Popover 向上展开，避免被窗口下边界裁切。

## 影响

用户进入聊天页即可识别当前工作区，并从同一入口进入工作区切换流程。
