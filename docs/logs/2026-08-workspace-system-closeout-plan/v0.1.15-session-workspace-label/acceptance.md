# Acceptance

1. 将默认工作区重置到 Diva profile-local `workspace`，不重启当前 runtime。
2. 确认仍在 `Pictures` 的当前 session 显示 `Pictures` 与对应路径，不显示“默认工作区”。
3. 在聊天入口重新选择当前 `Pictures` 目录，确认即使它与旧默认值相同，仍显示为显式目录。
4. 刷新状态或在设置页保存/重置默认目录后，当前 session 的实际路径和显式标签保持不变。
5. 新建聊天时，继续使用当前 session authority 的同一工作区；目录标签不得被默认配置文案覆盖。

## 人工验收结果

用户已确认上述工作区重置、当前 session 标签、显式同路径选择、刷新及新建聊天场景在真实桌面 GUI 中全部通过（2026-08-27）。
