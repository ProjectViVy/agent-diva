# Legacy Docs Reorg Summary

## Change

Consolidated the pending legacy documentation tree migration into the current `docs/dev` structure and removed the old tracked locations under `docs/evo-diva` and `docs/past/legacy-docs`.

## Impact

The repository keeps legacy and reference documentation inside the declared `docs/dev` hierarchy instead of leaving the working tree in a mixed old-path/new-path state.
