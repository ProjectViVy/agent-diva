# 验收

## 本片完成

- 打开桌面端不再在「建立 Persona」标题下直接显示 `unknown Laputa API error`。
- 未初始化时出现五份 Markdown 输入框（IDENTITY / RELATIONSHIP / REDLINE / USER / WORLD）。
- 提交合法五文件后状态变为 ready，对话门打开。

## 操作步骤

1. 重启当前 gateway（加载新 Manager 信封）。
2. 重启 / 重编桌面 GUI。
3. 用未初始化 profile 打开应用。
4. 确认引导框可填、无 `unknown Laputa API error`。
5. 填五份非空 Markdown 并完成设置。

## 仍挂人工

`PERSONA-S2-DESKTOP-SMOKE`：七文件工作区、CAS、pending、历史 Diff。
