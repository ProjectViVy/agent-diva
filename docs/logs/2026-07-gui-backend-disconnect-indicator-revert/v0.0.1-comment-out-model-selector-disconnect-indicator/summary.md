# Summary

- Commented out the extra backend-disconnected warning icon beside the top-right model selector in `NormalMode.vue`.
- Kept the existing topbar online/offline status indicator unchanged.
- Commented out the dedicated disconnected-indicator test block in `NormalMode.test.ts`.
