# 验收

## 自动验收

1. 普通手动触发在请求结束前得到明确终态，不保留永久 running 假象。
2. 有有效 session evidence 时只产生 Laputa proposal，不直接修改 Memory authority。
3. 无输入、超时与取消返回稳定、无载荷的失败码。
4. Evolution 页面明确显示当前仍处于建设中，且说明候选仍为规则式过渡实现。
5. 原有报告 trigger、取消、陈旧锁恢复和受限 worker 权限边界不回归。

## 最终人工验收

本切片不设置人工暂停点。用户无需在 E0 后测试；真实桌面验收统一由 G2D+ 执行。
