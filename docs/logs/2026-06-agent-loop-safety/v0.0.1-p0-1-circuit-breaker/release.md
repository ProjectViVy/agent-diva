# Release

No special release steps are required.

- This change is internal to `agent-diva-agent` and does not alter external command interfaces or configuration schema.
- Standard build/test validation is sufficient before the next normal release or preview cut.
