# Release

No special deployment or migration workflow is required beyond shipping the updated binaries.

- Config migration fills the new context budget defaults for migrated configs.
- Existing configs inherit defaults automatically through the Rust config loader.
