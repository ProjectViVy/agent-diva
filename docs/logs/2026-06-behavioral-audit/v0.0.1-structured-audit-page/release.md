# Release

- No separate deployment step was executed in this iteration.
- Backend/runtime release path remains the normal Rust workspace build and gateway packaging flow.
- GUI release path remains the existing Tauri packaging flow; this iteration validated the page with `npm run build` only.
