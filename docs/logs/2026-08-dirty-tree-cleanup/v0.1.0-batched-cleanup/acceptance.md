# Acceptance

1. 根目录不再存在 `.vibeyardignore`。
2. `LOCK.md` 的历史记录归档文件已入库，锁文件保持可用。
3. 12 份 crate-local `agents.md` 指南仍在对应 crate 目录中且已纳入版本控制。
4. `Cargo.toml` 未发生内容变更，仅清除了 Git 状态误报。
5. 释放本次清理锁后，工作树只应保留用户另行确认的未提交内容。
