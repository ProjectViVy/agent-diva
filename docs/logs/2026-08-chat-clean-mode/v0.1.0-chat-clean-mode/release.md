# 发布与加载

本迭代没有执行外部发布或推送。改动会随下一次 GUI/Agent 构建进入桌面包。

由于本次同时修改了 GUI 与 Agent 的工具结果持久化逻辑，部署到正在运行的桌面环境后需要重启 GUI 与 Gateway/Agent 进程，才能加载完整行为。当前验证过程中未结束或重启用户已有进程。

回滚方式：回退本迭代对应提交即可；localStorage 中新增的 `cleanMode` 字段没有破坏旧偏好格式，旧配置会按默认关闭处理。
