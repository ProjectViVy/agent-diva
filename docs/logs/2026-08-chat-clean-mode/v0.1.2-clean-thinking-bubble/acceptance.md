# 用户验收

1. 开启“通用 → 设置 → 聊天设置 → 清爽模式”。
2. 触发连续的思考和多个普通工具调用，确认只出现一个助手思考气泡，不出现多行工具气泡。
3. 确认气泡内只有一行“三个点 + 工具名”，多个工具名仍保持在同一行。
4. 确认思考正文、工具参数和工具输出不会显示；问答正文仍正常显示。
5. 关闭清爽模式，确认正常模式仍显示完整思考、工具名称、结果和详情入口。
6. 确认计划、TODO、审批交互卡片在清爽模式下仍保持可见和可操作。
