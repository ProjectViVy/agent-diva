# 清爽模式瞬时思考快照

本次将清爽模式的思考气泡收敛为“当前处理状态”的瞬时快照，避免过程消息在聊天记录中累积。

- 仅当思考、流式占位或工具调用仍处于活动状态时显示思考气泡。
- 深度思考在同一个气泡内显示为“三个点 + `深度思考：……`”，不使用括号，并允许 reasoning 文本流式更新。
- 气泡始终保持单行；当前运行的工具显示为 `工具调用：{name}`，不再用 `·` 拼接历史工具。
- 已完成的工具和思考过程不会留下静态气泡；问答正文开始渲染时，活动气泡立即消失。
- 普通工具输出仍被隐藏；正常模式和计划/TODO/审批交互卡片行为保持此前约定。
