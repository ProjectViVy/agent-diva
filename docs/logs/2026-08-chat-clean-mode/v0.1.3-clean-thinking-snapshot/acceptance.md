# 用户验收

1. 开启“通用 → 设置 → 聊天设置 → 清爽模式”。
2. 触发深度思考，确认思考气泡一次性出现，并以“三个点 + `深度思考：……`”格式流式更新内容，不带括号且始终只有一行。
3. 连续触发多个普通工具，确认气泡在三个点后显示 `工具调用：web_fetch` 这类当前活动工具说明，不出现 `·` 拼接的历史工具列表。
4. 工具或思考结束后，确认思考气泡消失；最终问答正文正常显示。
5. 确认工具参数和普通工具输出不会显示；计划、TODO、审批交互卡片仍可见且可操作。
6. 关闭清爽模式，确认正常模式仍显示完整思考、工具名称、结果和详情入口。
