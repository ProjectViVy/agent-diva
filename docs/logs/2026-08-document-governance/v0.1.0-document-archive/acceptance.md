# Acceptance

1. Open `docs/dev/improve/README.md` and confirm the review archive is discoverable.
2. Confirm the three review documents are under `docs/dev/improve/`.
3. Confirm `AGENTS.md`, `TODOLIST.md`, and the other declared root governance/package entry points remain at the repository root.
4. Confirm `git status` contains no unrelated source or generated-file changes.
