# Release

Ships with the normal Manager/CLI build. No schema migration command is needed;
the append-only governance table is created idempotently in the existing
workspace `.laputa` directory. No push or desktop deployment was performed.
