# 迭代摘要

- 目标：将 `agent-diva-gui` 右上角的“聊天 / 设置”入口移除，并统一归并到左侧抽屉导航。
- 实现：在 `src/components/NormalMode.vue` 中移除顶部 Tabs，新增抽屉内的“聊天 / 设置”一级入口，并让抽屉成为主导航入口。
- 导航逻辑：新增统一的 `navigateTo` 行为，聊天、设置、控制台、神经系统、Cron 共用一套切换逻辑。
- 交互补充：模型切换下拉中的“管理模型”现在也会走统一导航，直接进入设置页。
- 视觉补充：抽屉当前激活项增加高亮状态，聊天项保留消息数徽标。
- 国际化：新增 `nav.*` 文案，替换抽屉中的硬编码导航文本。

