# Summary

冻结 MEMRULES：不是人格文件，不进 Laputa。路径 `{config_dir}/memory/MEMRULES.MD`。
人在 Memory 设置窗口编辑；v1 只给人改。上下文对齐 GenericAgent L0：日常不进全文，
常驻最多几行指针，写记忆时才注入手册。不要和 L1 操作避坑 `[RULES]` 并成一份。

决策号：STM **S9**；Persona **P21**。无生产代码。
