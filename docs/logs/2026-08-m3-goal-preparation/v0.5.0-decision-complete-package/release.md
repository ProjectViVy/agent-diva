# Release

No deployment is required. Start a future interactive Codex session in this
workspace, read `docs/dev/governance-m3-goal/README.md`, and submit the objective
from `00-goal-brief.md` with `/goal`.

The Goal must not be started until the active writer has claimed `LOCK.md` and
rechecked the current branch, dirty worktree, backlog, and baseline tests.
