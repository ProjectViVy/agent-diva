# Release

- No migration is required.
- Ship with the next normal desktop GUI/Tauri build.
- Existing configs that already have an empty active model still need one manual re-selection of a valid model to repair persisted state.
