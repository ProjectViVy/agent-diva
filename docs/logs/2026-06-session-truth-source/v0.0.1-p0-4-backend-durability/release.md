# v0.0.1 P0-4 Backend Durability Release

No special deployment step is required.

- This change is a Rust workspace code update only.
- Normal release flow can ship it with the next workspace build after the remaining GUI truth-source slice is ready, or earlier if backend durability should be released independently.
