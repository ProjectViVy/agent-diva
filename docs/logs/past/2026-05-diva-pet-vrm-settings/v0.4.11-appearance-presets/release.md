# Release

No separate deployment step is required for this iteration. The change is part of the normal GUI build/package flow.

## Release Method

- Include the committed frontend and log changes in the next project build.
- Use the standard workspace release process after `just` validation passes or any unrelated failures are documented.
