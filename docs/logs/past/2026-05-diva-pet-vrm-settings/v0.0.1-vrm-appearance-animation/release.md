# 发布说明

本次改动属于 GUI 桌宠体验修复，不需要单独的数据迁移脚本。

## 发布方式

- 随 `agent-diva-gui` 下一次桌面应用构建一起发布。
- 自定义 VRM 模型保存在 `ConfigLoader` 对应配置目录下，不写入安装目录。

## 回滚说明

- 回滚本次提交即可恢复旧设置面板行为。
- 用户已导入的 `~/.agent-diva/vrm/models/custom/*.vrm` 文件不会被自动删除。
