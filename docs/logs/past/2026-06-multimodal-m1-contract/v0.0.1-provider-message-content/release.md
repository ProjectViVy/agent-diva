# M1 Provider Message Content Release

No independent release was performed for M1.

This change is a provider-layer type contract and compatibility update. It will ship with the next normal workspace release after the standard validation gate passes.

No migration is required for persisted sessions because `agent-diva-core::session::ChatMessage` remains text-only in this stage.
