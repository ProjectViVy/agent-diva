# Release

No separate release action is required.

This iteration adds focused regression tests and Sprint 4 closure documentation.
It does not change shipped runtime behavior beyond test-only inspection helpers.

The Windows LLVM PATH note is a developer environment clarification and will
ship with the normal documentation set.
