# Acceptance

1. Instantiate `GuardianConfig::default()`.
2. Confirm known-safe auto-approval and auto-learning are disabled.
3. Confirm permissive behavior remains available only through explicit non-default configuration such as `GuardianConfig::liberal()`.
