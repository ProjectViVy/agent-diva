# Release

- No application release step is required.
- The script is a repository-side developer utility; it only affects bundled metadata when a maintainer runs it and commits the resulting YAML diff.
