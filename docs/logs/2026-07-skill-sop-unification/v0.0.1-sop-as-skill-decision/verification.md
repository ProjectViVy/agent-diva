# Verification

- 核对现有 `SkillMetadata`、frontmatter 解析和 Skill 摘要路径。
- 核对参考项目中 SOP-as-Skill、Skills + Workflow、独立 SOP Runtime 三类设计。
- 检查决策覆盖格式、兼容、展示、实现范围、测试和非目标。
- 执行 `git diff --check` 验证文档格式。

本迭代无可执行行为变化，因此 Rust/GUI 测试不适用。

