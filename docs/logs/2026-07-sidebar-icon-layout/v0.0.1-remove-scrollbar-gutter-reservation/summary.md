# 侧边栏图标布局修复

## 变更

- 移除 `.scrollbar-thin` 的 `scrollbar-gutter: stable`。
- 保留细滚动条宽度、颜色和悬停样式。

## 原因

折叠侧边栏很窄时，稳定滚动条槽会从导航区域额外扣除一段宽度。菜单按钮与导航项的父级宽度因此不一致，导致截图中活动按钮明显变窄，图标看起来像被压缩。

## 影响

仅影响使用 `.scrollbar-thin` 的滚动区域布局，不改变图标组件尺寸或导航交互。
