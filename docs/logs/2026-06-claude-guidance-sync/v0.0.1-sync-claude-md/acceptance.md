# Acceptance

1. Open `CLAUDE.md` and confirm it states `AGENTS.md` is authoritative.
2. Confirm `CLAUDE.md` includes the current workspace crates, `.workspace/agent-diva-nano/` path, parallel lock/worktree rules, validation rules, and commit protocol.
3. Confirm `TODOLIST.md` records the sync under `Done`.
4. Confirm this iteration exists under `docs/logs/2026-06-claude-guidance-sync/v0.0.1-sync-claude-md/`.
