# Release

本迭代为文档与交接材料更新，不发布二进制、不切换生产 Manager、不合入 `dev`、不 push。

交付方式：

1. 在 `feat/channel-epic` 隔离 worktree 中完成文档变更。
2. 只 stage C5-P2 文档、迭代日志和对应 `TODOLIST.md` 变更。
3. 以英文 Conventional Commit 提交一个聚焦的 C5-P2 文档提交。
4. 保留 QQ blocked 和 C5-I/C5-V 未完成状态。
5. 释放 `LOCK.md`，记录 commit、验证命令、已知限制和接收 agent 下一步。
