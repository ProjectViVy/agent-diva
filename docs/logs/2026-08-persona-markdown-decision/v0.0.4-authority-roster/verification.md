# Verification

- 范围：文档对照；无 `just ci`（无生产代码变更）

对照：

- 决策记录 P1 表与 EPIC/TODOLIST/architecture 现行入口使用同一六文件名单
- `COMMITMENT` / `PREFERENCES` 在决策记录中仅作为被取代旧名出现
- 根 `USER.md` 与权威 `USER.MD` 在 P8 分开
- R3 README 标明表格是代码对象不是产品名
- 未改 crate / GUI
