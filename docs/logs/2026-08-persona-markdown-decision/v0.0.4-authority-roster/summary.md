# Summary

本次只修订产品决策与现行边界入口，没有修改产品代码。

- 权威文件定为六个全大写名：`IDENTITY.MD`、`RELATIONSHIP.MD`、`REDLINE.MD`、
  `USER.MD`、`DREAM.MD`、`WORLD.MD`；停用 Commitment / Preferences 作为产品名。
- `REDLINE.MD` 承载承诺与红线；`USER.MD` 承载用户偏好与 Agent 对用户的观察。
- `DREAM.MD` 是 Agent 自己的心愿，进 Frozen Core 且投影严格 10 字；不进首次引导；
  不是 AutoDream。
- 首次引导仍只原子直写五份用户侧权威；根目录旧 `USER.md` 不是新 `USER.MD`。
- 同步 EPIC、TODOLIST、现行架构边界、STM 决策 S6、R0/R3 指向，避免两套名单。
