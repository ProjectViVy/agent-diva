# 验收步骤

1. 打开中控台，确认不再显示日志面板。
2. 打开设置中的审计中心，确认可在三个页签间切换，并且日期选择器共享。
3. 在“网关后端日志”确认来源标记为后端网关进程；在“前端 GUI 日志”确认来源标记为桌面应用 WebView。
4. 在桌面应用控制台执行 `console.log`、`console.warn`、`console.error` 后刷新前端 GUI 日志，确认记录出现且敏感字段值被替换为 `[REDACTED]`。
