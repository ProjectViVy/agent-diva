# 迭代摘要

- 目标：将 `agent-diva-gui` 右上角的爱心图标、最小化按钮、关闭按钮注释掉，不再在标题栏显示。
- 实现：在 `src/components/NormalMode.vue` 的标题栏窗口控制区使用模板注释隐藏上述三个按钮。
- 清理：移除了不再使用的窗口 API 调用和相关脚本状态，避免构建时出现未使用告警。
- 影响范围：仅影响 GUI 标题栏右上角展示，不改变聊天、设置、模型切换、历史记录等功能。
