# Acceptance

用户原话要求：独立测试当前 Diva AutoDream；产品明确要修改和整理 STM，人格整理
可允许提案；问题错在不应该不测试。

验收点：

1. 独立跑了现有 AutoDream crate 测试与 Manager e2e，结果全绿并写入本日志。
2. 产品口径已改：STM 必须整理（直写）；人格只按 P19 表提案。
3. 表征测试记录「今天实际做什么」，而不是假装 STM 路径已存在。
4. 未借测试之名实现 STM 或接线新人格提案类型（Research Gate 仍有效）。
5. 诊断轨 `AUTODREAM-DIAGNOSTIC-LOGGING` 仍打开：缺阶段日志、缺对着产品表的测试。
