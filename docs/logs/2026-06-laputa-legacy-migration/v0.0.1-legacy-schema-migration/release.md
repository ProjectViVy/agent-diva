# Release

Date: 2026-06-14

## Method

No deployment artifact was produced in this iteration. The change is a Rust library capability inside `agent-diva-laputa` and will ship with the next workspace build/release.

## Notes

Migration remains explicit; no startup path silently mutates legacy authority files.
