# Release

This is an intermediate clean-break implementation slice, not a standalone release.

- No push or deployment performed.
- Recovery baseline remains the local branch `protect/cognitive-pre-clean-break-20260815` at `2aab18cc`.
- No migration or automatic deletion is provided; existing `.laputa/sections/*.json`
  and `.laputa/cognitive/WORLD.MD` files remain on disk and are not overwritten by
  the new startup path.
