# Acceptance

1. Doctor / config-status still omit `neuro-link` (intentional).
2. Future work starts from `NEURO-LINK-HEAVYWEIGHT-CHANNEL`, not from
   adding a telegram-style status block.
3. Existing neuro-link handler and localhost bind restriction remain.
