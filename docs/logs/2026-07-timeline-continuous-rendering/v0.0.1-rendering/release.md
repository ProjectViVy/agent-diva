# Release

Changes are merged directly into `agent-diva-pro`. The frontend component changes will take effect after restarting the web/desktop UI, and the backend changes will require a re-compilation of `agent-diva-cli` and `agent-diva-manager`.
