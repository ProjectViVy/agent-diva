# Release notes (internal)

- Plan mode demuxes proposed plans into durable reports without requiring perfect section headings first.
- Plan-mode prompt requires a Codex-style `<proposed_plan>` block when ready.
- Approval card shows soft incompleteness warnings; approve remains available for non-empty normalized plans.
