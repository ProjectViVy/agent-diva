# Release

- No special deployment step is required beyond the normal application update path.
- AutoDream report triggers and Notebook monthly generation use existing workspace storage locations, so release impact is limited to shipping the updated binaries.
