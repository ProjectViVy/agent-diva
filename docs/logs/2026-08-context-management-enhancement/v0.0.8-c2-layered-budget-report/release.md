# Release

No migration or feature flag is required. Existing compaction configuration
fields and provider wire types remain compatible. The change ships through the
normal workspace release process; no push was performed.
