# Acceptance

Start the rebuilt gateway from the real profile. It must survive runtime
assembly and serve a ready health response. A stack overflow, process exit,
typed Memory degraded state or fallback authority fails acceptance.
