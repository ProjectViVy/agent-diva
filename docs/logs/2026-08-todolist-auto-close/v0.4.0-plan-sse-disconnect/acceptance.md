# Acceptance

- [x] Plan 执行流（含 continue approved plan）走 `send_message` 的 `saw_terminal` 兜底。
- [x] 无独立 plan EventSource 循环在无终止事件时静默返回。
- [x] 未把 background / approval 重连改成单次 error emit。
- [x] 未改 `agent-diva-gui/src/**` Vue。
