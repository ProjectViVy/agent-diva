# Release

## Status

No publish was executed in this iteration.

## Notes

- The workspace is now aligned on `0.4.10` for first-party crates and release
  scripts.
- Follow the existing publish order once crates.io visibility is available:
  `files -> core -> tooling -> providers -> tools -> agent -> nano`.
