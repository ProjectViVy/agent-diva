# Verification

- `pnpm test -- src/components/NormalMode.test.ts` passed.
- `pnpm build` passed.

The focused regression test now asserts that the pet page hides the regular sidebar while keeping the main panel mounted.

