# Release — v0.0.1-gap-inventory

## 类型

**文档交付**。无二进制、无 crate 行为变更、无配置 schema 变更。

## 发布内容

路径：

```text
docs/logs/2026-08-05-memory-ga-parity-inventory/v0.0.1-gap-inventory/
  summary.md
  inventory.md
  acceptance.md
  verification.md
  release.md
```

可选：根 `TODOLIST.md` 增加 epic 指针。

## 部署方法

不适用（文档）。读者在仓库内打开上述路径即可。

## 回滚

删除该迭代目录并还原 `TODOLIST.md` 对应条目即可；无运行时回滚需求。

## 后续

实施须按 `inventory.md` Wave 0→5 推进，并在每个 Wave 单独开 iteration log；  
**禁止**在未冻结 §10 Open Questions 前大规模改 authority 写路径。
