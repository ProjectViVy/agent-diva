# Evolution Workspace Shell Release

Date: 2026-06-14

## Release Method

No deployment was performed in this iteration. The change is a GUI workspace shell update and will be released through the normal desktop GUI build/release flow after the pre-existing GUI build blockers are cleared.

## Rollout Notes

- Users will see a new Evolution sidebar item once the GUI build containing this change is shipped.
- The badge reflects existing Laputa proposal and event APIs.
- Detailed Inbox, Runs, Audit, and Policy behavior remains placeholder-only until subsequent stories are implemented.
