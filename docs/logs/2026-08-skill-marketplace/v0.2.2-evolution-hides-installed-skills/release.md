# v0.2.2 发布说明

## 发布方式

纯 GUI 改动,随下一次 GUI 重建发布生效(无需重建网关,但建议与 v0.2.1 网关同批发布)。

## 兼容性

- 依赖 v0.2.1 网关提供的 `evolution_managed` 字段。若新 GUI 连接旧网关(无该字段),
  Evolution 技能列表将为空并显示“尚无 Evolution 管理的 Skill。”;网关与 GUI 同包
  发布,属可接受降级。
- 无配置迁移、无数据格式变更。

## 回滚

还原本提交即可,无数据回滚需求。
