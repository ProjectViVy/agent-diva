# Acceptance

## Acceptance Steps
1. Start `agent-diva-manager` (or gateway).
2. Open `agent-diva-gui` and navigate to the Console Token Management tab.
3. Observe that the token statistics cards and breakdown are successfully displayed when the API responds successfully.
4. If the backend is turned off, observe that an error banner is displayed explaining the connection failure, rather than rendering an empty UI.
