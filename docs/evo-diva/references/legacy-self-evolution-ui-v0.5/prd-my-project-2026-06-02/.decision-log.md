# Decision Log

> PRD: agent-diva-pro 自进化 UI v0.5
> Created: 2026-06-02

## Session 2026-06-02

1. **P0 边界**：仅自进化侧（Chat 卡片 + 记事本 + 设置骨架 + Memory changelog）。Plan/Todo 降级为 Chat 内联卡片（pro 分支已有骨架）。
2. **治理页面命名**："记事本"，承载日报/周报/月报，不做宏观记忆拓扑图。
3. **双层审批模型**：即时审批在 Chat 内联卡片；节律治理在记事本（聊天触发提醒→跳转）。
4. **Todo 卡片**：通用 Chat 内组件，Plan 模式触发决策卡→同意→Todo 卡→逐项勾掉。
5. **侧边栏顺序**：Chat → 记事本 → Journal（原） → Pet → Settings → Console → Neuro → Cron。Journal 保留但不再扩展。
6. **目标分支**：agent-diva-pro（`C:\Users\Administrator\Desktop\morediva\agent-diva-pro`）。
7. **发布等级**：公开发布，100+ star 预期。
8. **进度由 PM 决策**：4 周迭代，3 次增量交付。
9. **沙箱 UI 并入**：沙箱设置并入 Settings → Self Evolution → 沙箱分段。沙箱审批走 Chat 即时审批卡片。参考沙箱分支 SandboxSettings.vue + sandbox.ts。不新建独立沙箱设置页。
10. **AutoDream 行为**：后台静默运行，用户不可见。
11. **PRD 封版**：2026-06-02，status → final。下阶段：UX 设计（bmad-ux）。报告产出写入 .laputa/rhythm/ 后通过 SSE 推送到 Chat 通知。
