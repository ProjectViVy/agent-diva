# Epic 4 回顾 — Person 体验、能力与可维护性

**项目：** newspace  
**史诗：** Epic 4 — Person 体验、能力与可维护性（FR8–FR11、FR17–FR18）  
**回顾日期：** 2026-03-31  
**参与者（叙事角色）：** Bob（Scrum Master）、Alice（Product Owner）、Charlie（Senior Dev）、Dana（QA）、Elena（Junior Dev）；项目牵头人 **Com01**

---

## 1. 史诗摘要与完成度

| 指标 | 结果 |
|------|------|
| 故事完成 | **4 / 4**（100%）：4.1、4.2、4.3、4.4 均为 `done` |
| 覆盖 FR | FR8、FR9、FR10、FR11、FR17、FR18（与 `epics.md` 一致） |
| 史诗状态（回顾后建议） | `epic-4` → `done`；`epic-4-retrospective` → `done` |

**交付主线：** Person 叙事 Vitest 基线与 `TESTING.md` 挂钩 → Settings 能力 manifest UI + Tauri IPC + `replace_with_manifest` 原子替换 → FR17 维护者文档 + FR21 ADR 交叉引用 + GUI README 双入口 → `doctor`/`status` 的 `--swarm` 挂点与版本化 `swarm_cortex` JSON。

---

## 2. 故事级深度分析（来自实现记录）

### 2.1 共性亮点

- **契约复用而非重复发明：** 4.2 严格消费 Story 1.6 的错误 DTO（`code` / `location_kind` / `path`）；4.4 在可传入 registry 时走 1.6 的 `summary()`，与规划一致。
- **评审闭环有效：** 4.1（`user-visible-app-root` 与神经切换后单壳）、4.2（clear+register 数据丢失 → `replace_with_manifest`）、4.3（对比表补「显式全蜂群 + 皮层关」、ADR 测试模块路径与对照表一致）、4.4（终端输出去重提取 `print_swarm_cortex_doctor_human`）均有可追溯 Review → 已解决记录。
- **文档与真值对齐：** 4.3 将 FR17 落在 `CORTEX_OFF_FR17_MAINTAINER_GUIDE.md`，与 1.4 登记文档、1.9 FR21 ADR 互链，满足「维护者可读 + 测试可追溯」。

### 2.2 反复出现的主题（流程与系统）

| 主题 | 证据 | 说明 |
|------|------|------|
| **跨 crate / 并行故事合流** | 4.2 Debug Log：为链接 `agent-diva-agent` 修补 swarm 编译；4.1 Review defer：i18n diff 含非本故事词条 | 与 Epic 3 类似：**Completion Notes / File List** 应显式标出「附带编译修复」范围，避免验收边界模糊。 |
| **测试深度与 E2E 缺口** | 4.2 defer：Panel + `invoke` 交互未测；4.1/4.2 均注明无 Playwright | 与 Epic 2/3 retro 一致：**Vitest 组件测**已扎实，**设置流 E2E** 仍为可选债。 |
| **预置 defer 项** | 4.2：`RwLock` poison 静默跳过；4.4：`doctor_report` 生产路径 `registry: None` | 已在故事中标注为 deferred / AC 允许 `unavailable`，但 **gateway 侧传入 registry** 仍是产品化前的衔接点。 |

### 2.3 技术债与已知后续工作

1. **能力 manifest：** `CapabilityManifestPanel` 与真实 `invoke` 的集成测试；`PlaceholderCapabilityRegistry::clear` 在锁异常时的可观测性（与 1.6 review 同族）。
2. **Doctor：** CLI 独立运行时能力摘要为 `unavailable`；需在持有 registry 的宿主（gateway）中传入 `swarm_cortex_doctor_section`（4.4 Review Defer 已写明）。
3. **Epic 3 retro A1：** 过程事件缓冲接入 `get_neuro_overview_snapshot` — **未在 Epic 4 故事清单内关闭**；4.3 仅触及 `neuro_overview` 类型/变体修复，不等同于 live 活动流。

### 2.4 上一史诗回顾（Epic 3）跟进情况

对照 [`epic-3-retro-2026-03-31.md`](epic-3-retro-2026-03-31.md)：

| Epic 3 行动/提醒 | Epic 4 执行情况 |
|------------------|----------------|
| A1 过程事件 → 神经快照 | **未闭环**；Epic 4 未以此为主目标 |
| 核对 Epic 4 是否假设神经 **live** | **无证据**表明 4.1–4.4 的 AC 依赖 live 活动列表；4.1 明确神经与主聊天 **互斥** 测单壳 |
| File List / 多史诗并线纪律 | **延续良好**（4.1、4.2 Completion Notes 列清范围） |
| E2E 策略 | **仍缺口**（4.2 明确跳过 Playwright 条件子任务） |

---

## 3. 下一史诗预览（Epic 5）

**状态：** 当前 `epics.md` 在 Epic 4 校验摘要后**未定义 Epic 5**。

**含义：** 回顾仍聚焦 Epic 4 学习与债务；下一批需求需经 PM/架构刷新路线图后再做「下一史诗准备」小节。

---

## 4. 回顾讨论纪要（合成版，Party Mode 摘要）

**Bob（Scrum Master）：** Epic 4 把「单一 Person 叙事可测」「能力在设置里闭环」「关皮层可维护说明」「doctor 挂点」四条线都落到可验收工件上，且大量依赖前置 Epic 1.6 / 1.4 / 1.9 的契约，说明前期投资在回报。

**Alice（Product Owner）：** FR8–FR11、FR17–FR18 在故事层可映射到测试或文档；用户向叙事与维护者向文档分轨清晰，符合 PRD 分工。

**Charlie（Senior Dev）：** 最大技术收获是 **IPC + 原子注册表替换** 与 **版本化 JSON 诊断块**；最大遗留仍是 **进程内 registry 与独立 CLI** 的鸿沟，以及 Epic 3 留下的 **神经快照 live 数据**。

**Dana（QA）：** 4.1 的 Vitest 策略让 FR8/FR9 有了机器守门；建议发版前仍执行故事内**手动走查表**路径 A/B。

**Elena（Junior Dev）：** 文档故事（4.3）也能带动代码侧小修复（`neuro_overview`），说明「纯文档」故事仍要做编译守护。

**Com01（Project Lead）：** 若需补充干系人验收或发布检查项，可记入站会或本文件附录。

---

## 5. 行动项（SMART，无工期数字 — 按项目节奏执行）

| # | 类别 | 行动 | 负责人 | 完成标准 |
|---|------|------|--------|----------|
| E4-A1 | 技术 | （延续 E3-A1）将过程事件接入神经快照，替换空切片 | Amelia / Dev | `commands.rs` 相关 TODO 消除；非空事件路径有契约或集成验证 |
| E4-A2 | 技术 | 在 gateway / 宿主路径为 `doctor_report` 或 status 收集 **传入** `PlaceholderCapabilityRegistry`（或等价），使能力摘要非仅 `unavailable` | Amelia / Dev | 至少一条集成或 E2E/手工脚本证明进程内摘要可读 |
| E4-A3 | 测试 | 可选：为 `CapabilityManifestPanel` 增加与 `invoke` 的浅层集成测（或 Playwright 设置流冒烟） | Quinn / Dev | 覆盖提交成功/失败各一；CI 可执行 |
| E4-A4 | 流程 | 路线图更新后 **起草 Epic 5** 或「维护/硬化」迭代条目，避免停在 Epic 4 后无跟踪载体 | John / Bob / Com01 | `epics.md` 或 backlog 中出现下一史诗或明确「暂停原因」 |

**团队约定（强化）：**

- **评审 Defer** 若影响产品化路径（registry、live 快照），须在 **sprint-status 或新故事** 中有键可查，不仅留在 Review 小节。
- **文档故事** 合并代码变更时，**File List** 须区分「文档-only」与「编译修复」行。

---

## 6. 显著发现与 Epic 5 计划影响

**是否需立即改写未定义的 Epic 5：** 否（Epic 5 尚不存在）。

**需在下一波规划时带入的假设修正：**

- 独立运行 `agent-diva config doctor` 时，蜂群/能力块可能长期为 **结构化 unavailable** —— 若产品期望「开箱即有能力计数」，需明确 **是否必须长驻 gateway**。
- 神经活动 **live** 与 Epic 4 交付 **正交**；若未来故事以「实时神经列表」为 AC，须先完成 E3/E4-A1 类接线或显式接受 stub。

---

## 7. Epic 4 就绪度核对

| 维度 | 评估 | 备注 |
|------|------|------|
| 故事与记录中的测试 | 通过 | 各故事记录 `npm test` / `cargo test` / clippy 等 |
| 手动验收 | 待 Com01 | 4.1 走查表须版本发版前勾选 |
| Doctor 能力摘要（无宿主） | 部分 | 设计内 `unavailable`，非回归失败 |
| 部署/干系人 | 未在故事中记录 | 由 Com01 按发布流程确认 |

---

## 8. 关键收获（带走三条）

1. **前置契约（1.6、1.4、1.9）是 Epic 4 的加速器** — UI、文档、CLI 挂点均可薄层对接而非重写领域逻辑。  
2. **原子替换与版本化 JSON** 是跨端一致性的实用模式（注册表、`SwarmCortexDoctorV1`）。  
3. **并行合流时 i18n / 编译牵连** 应用 File List + defer 标注管理预期，减少「看起来像范围蔓延」的摩擦。

---

## 9. 下一步

1. 执行行动项表（按优先级：**E4-A1** 若产品要 live 神经；**E4-A2** 若要 CLI/GUI 一致诊断体验）。  
2. 站会确认负责人与完成标准。  
3. 定义 Epic 5 或维护迭代范围后，再跑一次「下一史诗准备」专项。

---

*本文件由 BMAD Retrospective 工作流依据 `sprint-status.yaml`、`epics.md` 与 Epic 4 故事文件生成；用户指定回顾 **Epic 4**。*
