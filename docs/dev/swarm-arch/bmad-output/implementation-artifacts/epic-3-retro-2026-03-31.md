# Epic 3 回顾 — 神经系统观测视图（MVP 分期）

**项目：** newspace  
**史诗：** Epic 3 — 神经系统观测视图（MVP 分期）  
**回顾日期：** 2026-03-31  
**参与者（叙事角色）：** Bob（Scrum Master）、Alice（Product Owner）、Charlie（Senior Dev）、Dana（QA）、Elena（Junior Dev）；项目牵头人 **Com01**

---

## 1. 史诗摘要与完成度

| 指标 | 结果 |
|------|------|
| 故事完成 | **4 / 4**（100%） |
| 覆盖 FR | FR5、FR6、FR7、FR15、FR16（与 `epics.md` 一致） |
| 史诗状态（回顾后） | 全部故事 `done`，回顾已登记完成 |

**交付主线：** 侧栏进入 `NervousSystemView` → `BrainOverview`（左右分区 + 架构图式）→ `NeuroDetailPanel` + `DataPhaseBadge`（诚实数据阶段）→ 空/错/闲排障模板（`NeuroTroubleshootCallout`）→ 愿景折叠区（FR16，非必经）。

---

## 2. 故事级深度分析（来自实现记录）

### 2.1 共性亮点

- **契约先行、诚实标注：** 3.2 在 `agent-diva-swarm` 引入 `NeuroOverviewSnapshotV0` / `NeuroDataPhase`，与 `CortexState` 同源；桌面端在过程事件未接入时固定空切片，**主动**呈现 `stub`/`degraded`，避免假列表冒充实时（FR6、UX-IMPL-4）。
- **评审闭环扎实：** 3.1（`#neuro-detail-panel-root`、`cortex_toggled` try/catch、File List / 并线 i18n 说明）、3.3（非 Tauri 下隐藏「关闭皮层」、清理未用文案键）、3.2（工具 error 与 `message` 子串约定写进 Rust 注释）均有可追溯的 Review Findings → 已解决/已拍板。
- **可测性：** Vitest 覆盖神经 API、面板、角标、愿景 i18n；Rust 侧有快照构建与契约测试；3.3 为三种排障态提供 `data-testid`。

### 2.2 反复出现的主题（可作为流程改进输入）

| 主题 | 证据 | 说明 |
|------|------|------|
| **跨史诗并线变更** | 3.1 Completion Notes：`cortex.*`、`settings.capabilityManifest` 与 neuro 同批 | 需在故事或 PR 中**显式列出 File List**，避免「范围看起来只有 neuro」的验收歧义。 |
| **MVP 与后续工作的显式切分** | 3.2 Code review **选项 1**：过程事件缓冲接入 `get_neuro_overview_snapshot` **顺延** | 决策正确（诚实 stub），但 `commands.rs` 仍留有 **TODO**（见下节技术债）。 |
| **运行环境差异** | 3.3：浏览器预览 vs Tauri | 建议动作须按 `isTauriRuntime()` 等分支，避免静默无反馈。 |

### 2.3 技术债与已知后续工作

1. **`get_neuro_overview_snapshot`：** 当前传入空过程事件切片；需在 gateway / AgentLoop 侧挂载 `ProcessEventRecorder`（或等价）后改为真实切片（`commands.rs` 内 TODO + 文档注释已说明）。
2. **3.1 故事正文头部仍写 `Status: review`**：与 frontmatter `status: done` 略不一致，属文档卫生问题，可顺手修正以免后人误读。

### 2.4 上一史诗回顾（Epic 2）

已对照 [`epic-2-retro-2026-03-31.md`](epic-2-retro-2026-03-31.md)：

| Epic 2 关注点 | Epic 3 执行情况 |
|---------------|----------------|
| 神经视图与聊天区 **同源、不矛盾** | 快照经 `CortexState` + 诚实 `stub`/`degraded` 落实；**过程事件 → 神经快照** 仍待接线（与 E2 准备的「单一真相源」一致，属有意识切割）。 |
| 皮层 / gateway 过程门控同步债（E2 A3） | 未在本史诗内关闭；观测视图通过 **数据阶段角标** 降低误判，但 **live** 活动行仍依赖后续 `ProcessEventRecorder` 接入。 |
| E2E 策略（E2 A1） | Epic 3 以组件测 + 契约为主；全链路 E2E 仍为跨史诗缺口。 |
| 排障语气与 2.2/2.3 一致（E2 §10 准备项） | 3.3 模板化文案 + Tauri/浏览器分支，与「诚实反馈」方向一致。 |

---

## 3. 下一史诗预览（Epic 4）

**Epic 4：Person 体验、能力与可维护性** — FR8–FR11、FR17–FR18 等。

**与 Epic 3 的衔接：**

- 神经系统入口与可访问性（NFR-A1 等在规格中与神经入口相关）已在 Epic 3 落地；Epic 4 可在此基础上继续做 Person 叙事、能力 UI、文档与 doctor 挂点。
- **依赖提醒：** 若 Epic 4 故事需要「神经详情与聊天过程**完全同源实时列表**」，须优先推进上述 **过程事件 → 神经快照** 接线，否则仍为诚实 stub 阶段。

**显著发现（是否需改 Epic 4 计划）：** 无证据表明 Epic 4 故事清单基于错误假设；**但** 若某故事假设神经面板已是 **live** 活动流，需对照当前 TODO 调整验收或拆出跟进故事。

---

## 4. 回顾讨论纪要（合成版，侧重系统而非个人）

**Bob（Scrum Master）：** Epic 3 从「占位页」走到「可导航、可理解、可排障」的 MVP，且全程遵守 FR16 与 UX-DR2，这是系统性的胜利。

**Alice（Product Owner）：** FR5–FR7、FR15 在实现上与 PRD/UX 对齐；诚实数据阶段反而增强了信任，而不是削弱「功能感」。

**Charlie（Senior Dev）：** 最大技术收获是 **Rust 快照 + Tauri command + Vue** 的单向数据流；最大遗留是 **过程事件缓冲** 尚未接到神经快照，这是有意识的 MVP 切割，不是疏忽。

**Dana（QA）：** 空/错/闲分态测试与 `data-testid` 让回归成本可控；建议保持「错态不堆原始日志」的门禁。

**Elena（Junior Dev）：** 多文件 i18n 并线时，Completion Notes 里写清范围很有帮助。

**Com01（Project Lead）：** 若你有额外「上线前必做」或干系人反馈，请记入下一次站会或补一段到本文件「附录」。

---

## 5. 行动项（SMART，无工期数字 — 按项目节奏执行）

| # | 类别 | 行动 | 负责人 | 完成标准 |
|---|------|------|--------|----------|
| A1 | 技术 | 将 **过程事件缓冲** 接入 `get_neuro_overview_snapshot`（替换空切片），与 Story 1.5 / gateway 同源 | Amelia / Dev | `commands.rs` TODO 消除；契约测试覆盖非空事件路径；GUI 在皮层开且有事时可见与过程条一致的语义 |
| A2 | 文档 | 修正 `3-1-nervous-system-shell.md` 正文 `Status: review` → 与 frontmatter 一致 | Bob / SM 或 Dev | 文件内状态字段一致 |
| A3 | 流程 | **新建故事或任务** 跟踪 A1，避免只在代码注释中「隐形欠债」 | John / PM 或 Bob | backlog 或 implementation-artifacts 中有可跟踪键 |
| A4 | 流程 | Epic 2 若仍无书面回顾，**补写简短 epic-2-retro**（可选）或从 Epic 3 起只向前维护 | Bob / Com01 | 团队明确「回顾文件即承诺载体」 |

**团队约定（非正式）：**

- 多史诗同 PR：在 **Completion Notes** 中列出全部受影响 epic/故事键。
- 评审拍板「顺延」项：必须在 **sprint-status** 或 **deferred-work** 或新故事中可见，而不仅是 `// TODO`。

---

## 6. Epic 3 就绪度核对（故事完成 vs 生产就绪）

| 维度 | 评估 | 备注 |
|------|------|------|
| 故事与测试 | 通过 | 记录中 `npm test` / `npm run build`、Rust 测试均有执行说明 |
| 数据真实性 | 部分 | 神经活动行为 `stub`/`degraded` 为**设计内诚实状态**，非 bug |
| 部署/干系人 | 未在故事中记录 | 由 Com01 按发布流程自行确认 |

---

## 7. 关键收获（带走三条）

1. **诚实 stub 是可交付 MVP 的一部分**，只要在 UX 与 API 层与聊天侧同源、且文档与角标一致。  
2. **Code review 与 File List 纪律** 在多史诗并行时降低范围争议。  
3. **环境感知（Tauri vs 浏览器）** 应进入排障与建议动作的默认设计清单。

---

## 8. 下一步

1. 执行行动项表（尤其 **A1** 若产品期望「实时」神经活动）。  
2. 在站会上过一遍负责人与完成标准。  
3. 继续 Epic 4 时，核对故事是否假设神经快照已为 **live** 模式。

---

*本文件由 BMAD Retrospective 工作流依据 `sprint-status.yaml`、`*epic*.md` 与 Epic 3 故事文件生成。*
