# Release

- No special release procedure is required for this iteration.
- Ship with the next normal desktop build/package so the updated provider catalog and General settings page are included together.
