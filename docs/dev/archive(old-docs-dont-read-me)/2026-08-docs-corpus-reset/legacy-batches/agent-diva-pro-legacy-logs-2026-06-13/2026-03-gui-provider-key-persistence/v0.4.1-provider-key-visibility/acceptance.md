# Acceptance

1. Open GUI settings and enter `频道`.
2. Confirm the sidebar activation control is a switch instead of a text button.
3. Confirm the switch only changes the enabled draft state and does not change the selected channel.
4. Open `供应商` and confirm saved API keys are present after reopening the settings view.
5. Confirm the eye button toggles API key visibility between masked and plain text.
6. Save provider changes and confirm reopening the app still shows the saved API key value from local config.
