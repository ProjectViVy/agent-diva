# P4 Summary

The approval UI blocks incomplete plans, exposes TODO policy choices, consumes the backend execution state, and continues execution through a dedicated hidden continuation command instead of a visible synthetic user message.
