# Summary

- Added a backend-disconnected warning icon beside the top-right model selector in `NormalMode`.
- Reused the existing frontend health state and only show the indicator when `connectionStatus === 'error'`.
- Added localized tooltip text in English and Chinese.
- Added targeted component coverage for icon visibility, tooltip wiring, and dropdown interaction.
