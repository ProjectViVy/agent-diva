# Acceptance

1. 打开 GUI 主聊天页，底部预算圆环显示随当前会话内容变化的真实百分比，不再固定为 `50%`。
2. 进入“设置 -> 压缩”，预算状态区显示当前会话 `history_estimated / history_budget` 和压缩状态。
3. 在压缩设置里修改 `max_tokens`、阈值或 `keep_recent_count` 后保存，刷新设置页仍能读到新值。
4. 返回聊天页，预算圆环按新的 `tools.budget` 配置重新计算。
5. 当当前会话仅有本地欢迎占位消息时，预算状态显示为空会话压力，不把欢迎语计入预算。
