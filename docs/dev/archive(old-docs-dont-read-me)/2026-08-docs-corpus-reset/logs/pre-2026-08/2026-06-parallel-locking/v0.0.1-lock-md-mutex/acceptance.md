# Acceptance

1. Open the repository root and confirm `LOCK.md` exists.
2. Read `LOCK.md` and verify it includes owner, scope, heartbeat, expiry, and handoff fields.
3. Read `AGENTS.md` and verify there is a `Parallel Lock Mechanism` section plus a mandatory `parallel-lock-file-required` rule.
4. Confirm `TODOLIST.md` records the mutex-process update under `Done`.
