# Release

Changes are merged directly into `agent-diva-pro`. The frontend component changes will take effect after restarting the web/desktop UI, and the manager needs to be recompiled to expose the `minute` intervals.
