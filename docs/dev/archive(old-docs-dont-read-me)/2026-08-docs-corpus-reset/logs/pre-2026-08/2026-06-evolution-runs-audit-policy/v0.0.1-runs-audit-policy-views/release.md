# Release

No release was performed.

Reason: Story 2.4 target GUI tests pass, but full workspace validation is still blocked by pre-existing unrelated failures. The change should be reviewed after those blockers are resolved or explicitly waived.
