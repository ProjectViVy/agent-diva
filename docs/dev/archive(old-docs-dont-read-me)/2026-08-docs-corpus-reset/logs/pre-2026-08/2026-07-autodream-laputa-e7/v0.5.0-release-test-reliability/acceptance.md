# Acceptance

The gate is accepted when invalid-session reconnect deterministically identifies,
the logging test cannot recursively acquire Cargo's lock, and the embedded
gateway reports a real startup result before loopback health is probed.

All three focused scenarios pass. Full-workspace release acceptance remains E7
work and does not substitute for the deferred final desktop validation.
