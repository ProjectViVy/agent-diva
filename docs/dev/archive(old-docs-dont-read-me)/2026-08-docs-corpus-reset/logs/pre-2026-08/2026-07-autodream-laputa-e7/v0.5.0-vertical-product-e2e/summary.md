# Automated Vertical Product E2E

The automated product path now proves:

Experience evidence → recoverable AutoDream worker → deterministic reflection →
governed Laputa proposal → one-time approval → typed Memory apply → Recall
selection → payload-free feedback → typed rollback → Recall disappearance.

The test exposed and fixed two production defects: typed finalization incorrectly
required the legacy JSON projection format, and existing canonical stores were
reopened read-only before later authority writes.
