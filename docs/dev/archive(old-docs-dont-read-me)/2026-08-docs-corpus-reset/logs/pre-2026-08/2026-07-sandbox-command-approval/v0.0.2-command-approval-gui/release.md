# Release

No deployment or remote push was performed. The change is recorded in local Conventional Commits and becomes available in the next desktop build.

No configuration migration is required. Existing Sandbox configuration and policy files are unchanged. Rollback consists of reverting the GUI workflow commit and this documentation commit; the backend approval API remains backward compatible.
