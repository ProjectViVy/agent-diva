# v0.0.1 TODOLIST and Clean Commit Policy Acceptance

## Acceptance Criteria

- `TODOLIST.md` exists and contains the current GUI image paste optimization item.
- `AGENTS.md` requires future bugs, gaps, and unfinished work to be added to `TODOLIST.md`.
- `AGENTS.md` requires cleanup or exclusion of current-task scratch artifacts before committing.
- The commit policy still avoids staging unrelated user changes and still forbids pushing without explicit request.

## Result

Accepted as a documentation and workflow-policy update.
