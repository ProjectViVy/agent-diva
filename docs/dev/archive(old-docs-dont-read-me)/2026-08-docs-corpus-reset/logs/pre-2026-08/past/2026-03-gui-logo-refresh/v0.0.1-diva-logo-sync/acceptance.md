# Acceptance

1. Open `agent-diva-gui/src-tauri/icons` and confirm the generated icon files were refreshed after `DIVA.jpg` was added.
2. Build or package the GUI app and confirm the executable/app bundle icon now matches `DIVA.jpg`.
3. Confirm that the splash screen and About page remain unchanged.
