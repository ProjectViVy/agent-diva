# Release

No standalone release artifact for this iteration.

This change ships as part of the normal Agent-Diva workspace release cycle after Sprint 7 A6/A7 validation completes.

Deployment note: builds without the `mentle` Cargo feature persist configuration and expose GUI controls, but dynamic toolkit discovery and live Mentle runtime refresh require a Mentle-enabled agent build.
