# Acceptance

1. Build a `SandboxCommand` with arguments containing shell metacharacters.
2. Verify the generated command string preserves those characters as literal argument data.
3. Confirm direct execution does not run an injected secondary command.
