# Acceptance

1. Rebuild and relaunch the desktop app.
2. Open `Settings -> Providers`.
3. Confirm `Mimo` appears only once even if the running backend API still exposes duplicate builtin/custom rows.
4. Confirm selecting `Mimo` still shows the configured API base and key state.
5. Confirm `General -> Clear Cache` remains available and does not affect provider credentials.
