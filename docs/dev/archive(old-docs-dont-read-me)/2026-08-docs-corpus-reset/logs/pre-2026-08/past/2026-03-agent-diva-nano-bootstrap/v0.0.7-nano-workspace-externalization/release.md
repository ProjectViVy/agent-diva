# v0.0.7 Release

No release is performed in this iteration.

Reason:

- This round only externalizes nano from the main workspace cargo graph.
- The `external/agent-diva-nano/` directory is intentionally kept in-repo until manual relocation.
- Future nano release work must happen from the external nano repository.
