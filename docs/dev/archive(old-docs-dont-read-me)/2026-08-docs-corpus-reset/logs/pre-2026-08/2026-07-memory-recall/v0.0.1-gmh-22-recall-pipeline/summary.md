# GMH-22 Iteration Summary

Added a stable Recall v2 request/candidate/source contract, fail-closed staged
selection, deterministic scoring and decay, supersession and digest
deduplication, hard token budgeting, safe rendering, content-free traces, and
shadow comparison.

Mentle/Hybrid search hits without applied provenance are explicitly untrusted.
The existing provider prefetch and production prompt path are unchanged.
