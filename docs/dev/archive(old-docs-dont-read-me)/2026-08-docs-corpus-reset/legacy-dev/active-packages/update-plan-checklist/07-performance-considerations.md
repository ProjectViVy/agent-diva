# 性能考量

## 复杂度

GUI 每次 checklist/final 事件最多反向扫描一次当前消息数组，时间复杂度 O(n)、额外空间 O(1)。清单最大 20 项，序列化开销受现有工具校验限制。

## 异步路径

事件顺序调整不增加 await、锁或阻塞调用；仍通过既有 unbounded channel 与 message bus 发布。状态别名由 serde 常量匹配完成。

## 优化边界

无需索引、缓存或基准测试。若未来单会话消息达到大规模，应将 request ID 记录到消息模型并按 ID 定位；当前不为未发生的规模问题增加复杂度。
