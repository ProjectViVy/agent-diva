# Acceptance

1. 打开 GUI，进入“设置”首页，确认存在独立 `MCP` 卡片入口，且不在“通用”页内部。
2. 进入 MCP 页面，确认顶部展示总数、在线数、异常数、禁用数，以及“当前使用”区域。
3. 通过表单新增一个 `stdio` 或 `http` MCP，保存后列表中出现该项，并能看到状态徽标与工具数量/错误信息。
4. 对已有 MCP 执行编辑、启用/禁用、删除，确认列表即时更新。
5. 导入 JSON 文件，确认单个 MCP 或 MCP 字典都能落入列表。
6. 修改 MCP 后无需重启应用，后端运行时应立即刷新 MCP 工具集合。
