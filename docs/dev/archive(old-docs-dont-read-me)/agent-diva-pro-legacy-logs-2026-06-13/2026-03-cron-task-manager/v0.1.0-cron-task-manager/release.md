# Release

- No separate deployment step was executed in this iteration.
- The change is ready to ship as part of the normal workspace release flow after review.
- Runtime expectation:
  - start the gateway
  - open `agent-diva-gui`
  - navigate to the new scheduler page from the sidebar
  - verify cron CRUD and lifecycle operations against the local gateway API
