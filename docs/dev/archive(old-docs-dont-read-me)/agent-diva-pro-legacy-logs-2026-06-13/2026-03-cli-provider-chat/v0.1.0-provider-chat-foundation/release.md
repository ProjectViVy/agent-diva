# Release

- No separate deployment step was executed in this iteration.
- This change ships as normal Rust workspace code and docs updates.
- If releasing binaries, use the repository's normal CLI packaging flow after CI passes.
