# Verification

## Commands

- `cargo test -p agent-diva-sandbox platform::macos::tests::test_execute_returns_err_when_sandbox_exec_unavailable`

## Result

- Not executed in the current Windows environment because the macOS module is target-gated.
