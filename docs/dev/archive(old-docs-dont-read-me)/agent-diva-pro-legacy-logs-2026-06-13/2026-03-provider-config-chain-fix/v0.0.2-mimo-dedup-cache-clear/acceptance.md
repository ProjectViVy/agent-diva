# Acceptance

1. Open the GUI settings and enter `Providers`.
2. Confirm builtin providers such as `Mimo` appear only once even if shadow config exists for the same provider ID.
3. Open `General` and click `Clear Cache`.
4. Confirm the success hint appears and provider credentials remain intact after returning to `Providers`.
5. Rebuild/relaunch the desktop app and verify the provider list still contains a single `Mimo` entry.
