# 验收（用户视角）

- [ ] 用户曾在 GUI 聊过天，关闭应用后再次打开，**默认看到上次那条 GUI 聊天的内容**（按会话文件最近更新时间排序取最新 `gui:` 会话），而不是空白新聊天。
- [ ] 仅有其它频道（如 Telegram）会话更新时，GUI 启动仍打开 **GUI 侧**最近会话，而不是把其它频道会话加载进桌面聊天界面。
- [ ] 没有任何 `gui:` 会话文件时，行为与从前一致（欢迎引导 + 新聊天）。
