# Release

## Version
- V0.4.0 PREAUDIT

## Steps
1. Confirm the iteration logs and documentation (this summary, verification, acceptance, and docs/windows-standalone-app-solution.md) are committed so auditors can trace the scope of the work.
2. Run the remaining pre-audit validation (just check, just test, GUI smoke flows) on an environment that already has just installed before tagging or shipping.
3. Package the Windows standalone artifacts per the documented guide and archive them alongside the log bundle for the release candidate.
