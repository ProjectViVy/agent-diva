# Release

- Not released.
- This iteration prepares `agent-diva-nano` for independent publication but does not execute crates.io publish.
- Actual release still requires topo-ordered publishing of the internal dependency chain.
