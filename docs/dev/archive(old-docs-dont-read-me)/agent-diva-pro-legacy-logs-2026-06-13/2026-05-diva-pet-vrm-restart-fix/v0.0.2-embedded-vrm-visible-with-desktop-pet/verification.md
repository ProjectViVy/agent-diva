# Verification

1. `pnpm test`
   - Result: passed

2. `pnpm build`
   - Result: passed

## Smoke expectation

- With desktop pet mode enabled, opening the Diva Pet tab should still show the embedded VRM viewer.
- A small active-state badge should remain visible so the user knows the floating desktop pet is also running.
