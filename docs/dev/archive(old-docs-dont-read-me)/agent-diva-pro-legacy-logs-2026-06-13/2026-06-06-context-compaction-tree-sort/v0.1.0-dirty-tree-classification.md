# 脏树分类报告：feature/context-compaction

> **日期**: 2026-06-06
> **分支**: `feature/context-compaction` (pro repo, HEAD=89e0673)
> **基准**: 9 个已修改文件 + 12 个未跟踪文件 = 21 个变更文件
> **路由规则**: `MOREDIVA-路由治理.md` (FROZEN)

---

## 分类总览

| 类别 | 文件数 | 操作 |
|------|--------|------|
| **回流 main** — runtime/infrastructure | 15 | Cherry-pick 到 agent-diva main |
| **留在 pro** — product planning docs | 5 | 保留在 feature 分支，合入 pro |
| **归档 selfinprove** — research | 2 | 移到 agent-diva-selfinprove |
| **丢弃** — 纯格式化噪音 | 3 | `git checkout HEAD -- <file>` |

---

## 1. 回流 main（15 个文件）

Context Compaction 是底层 runtime/budget/safety 能力，治理规则明确允许进入 main。

### 1A. 核心模块（新文件，6 个）

| 文件 | 行数 | 功能 |
|------|------|------|
| `agent-diva-agent/src/compaction/mod.rs` | 28 | 模块入口，re-export |
| `agent-diva-agent/src/compaction/compaction_exec.rs` | 281 | ContextCompactor — LLM 驱动的上下文压缩执行器 |
| `agent-diva-agent/src/compaction/prompt.rs` | 31 | COMPACTION_SYSTEM_PROMPT |
| `agent-diva-agent/src/context_budget.rs` | 335 | BudgetConfig + check_budget() |
| `agent-diva-agent/src/token_estimate.rs` | 238 | TokenEstimator (chars/4 × 4/3) |
| `agent-diva-agent/tests/compaction_integration.rs` | 461 | 集成测试 |

### 1B. 集成修改（已修改文件，6 个）

| 文件 | 改动 | 功能 |
|------|------|------|
| `agent-diva-agent/src/agent_loop/loop_turn.rs` | +270/-10 | Agent loop 集成 budget check + compaction 触发 |
| `agent-diva-agent/src/context.rs` | +20/-2 | build_messages() 接受 session_compaction 参数 |
| `agent-diva-agent/src/lib.rs` | +3 | 注册 compaction/context_budget/token_estimate 模块 |
| `agent-diva-core/src/session/store.rs` | +80/-0 | CompactSummary schema + Session 新增字段 |
| `agent-diva-core/src/session/manager.rs` | +13 | JSONL 序列化 last_compacted + compaction |
| `agent-diva-core/src/session/mod.rs` | +2/-1 | re-export CompactSummary/CompactTrigger/CompactionRange |

### 1C. 提交策略

建议拆成两个 Conventional Commit：

1. **`feat(runtime): add context compaction engine`**
   - 包含 1A 全部 + store.rs + manager.rs + mod.rs
   - 纯数据模型 + budget 逻辑，不涉及 agent loop

2. **`feat(agent): integrate compaction into agent loop`**
   - 包含 loop_turn.rs + context.rs + lib.rs
   - 依赖第一个 commit

---

## 2. 留在 pro（5 个文件）

产品规划文档，pro 作为产品线的 landing zone。

| 文件 | 行数 | 功能 |
|------|------|------|
| `docs/adr/0010-context-compaction.md` | 788 | ADR — Context Compaction 架构决策 |
| `docs/epics/context-compaction-epics.md` | 964 | Epic & Story 分解 |
| `docs/prds/prd-agent-diva-pro-2026-06-03/prd.md` | 174 | PRD |
| `docs/prds/prd-agent-diva-pro-2026-06-03/.decision-log.md` | 35 | 决策日志 |

**提交**: `docs: context compaction ADR, PRD, and epic decomposition`

---

## 3. 归档 selfinprove（2 个文件）

Thinking-mode 研究文档，路由规则禁止纯研究文档进入 pro。

| 文件 | 行数 | 功能 |
|------|------|------|
| `docs/research/agent-diva-thinking-injection-points.md` | 437 | Thinking mode injection points 分析 |
| `docs/research/cherry-studio-thinking-analysis.md` | 1019 | Cherry Studio thinking 架构研究 |

**操作**: 从 pro 删除，提交到 agent-diva-selfinprove 的对应位置。

---

## 4. 丢弃（3 个文件）

纯格式化/空白修改，与 context-compaction 无关，不构成有意义的变更。

| 文件 | 改动内容 |
|------|----------|
| `agent-diva-agent/src/subagent.rs` | +4/-2 — 仅换行对齐 |
| `agent-diva-gui/src-tauri/src/commands.rs` | +6/-6 — 仅行尾格式 |
| `agent-diva-tools/src/attachment.rs` | +2/-1 — import 排序 |

**操作**: `git checkout HEAD -- <file>` 恢复原状。

---

## 5. 执行顺序

```
Step 1: 丢弃噪音文件
        git checkout HEAD -- agent-diva-agent/src/subagent.rs
        git checkout HEAD -- agent-diva-gui/src-tauri/src/commands.rs
        git checkout HEAD -- agent-diva-tools/src/attachment.rs

Step 2: 归档 thinking research → selfinprove
        (从 pro 删除，移至 selfinprove)

Step 3: 留在 pro — 提交 product planning docs
        git add docs/adr/ docs/epics/ docs/prds/
        git commit -m "docs: context compaction ADR, PRD, and epic decomposition"

Step 4: 回流 main — cherry-pick runtime 代码到 agent-diva main
        (在 agent-diva main 创建 feature/context-compaction-runtime 分支)
        (cherry-pick 或手动搬运 1A+1B 文件)
        (拆成两个 commit 如 1C 所述)

Step 5: 从 pro 分支移除已回流的 runtime 文件
        (这些文件应存在于 main 而非 pro)
```

---

## 6. 决策记录

| # | 决策 | 理由 |
|---|------|------|
| D1 | compaction runtime → main | 治理规则第 2.1 节明确列出 "Context compaction 底层 runtime / budget / safety 能力" |
| D2 | PRD/ADR/Epics → pro | 产品规划文档属于 pro landing zone |
| D3 | thinking research → selfinprove | 治理规则 2.2 节禁止 "纯研究文档或架构探索" 进入 pro |
| D4 | 格式化改动 → 丢弃 | 与 context-compaction 无关，避免混合提交 |
| D5 | runtime 拆两个 commit | 核心模块 + loop 集成是两个独立关注点 |
