# Acceptance

1. Open `docs/userguide.md`.
2. Confirm there is a clear start-to-finish CLI workflow for a new user.
3. Confirm each major command group is documented: `onboard`, `config`, `status`, `agent`, `chat`, `tui`, `gateway`, `provider`, `channels`, `cron`, `service`.
4. Confirm examples include explicit `--config`, `--config-dir`, `--workspace`, and `--remote` usage.
5. Confirm the README Documentation section links to the new guide.
