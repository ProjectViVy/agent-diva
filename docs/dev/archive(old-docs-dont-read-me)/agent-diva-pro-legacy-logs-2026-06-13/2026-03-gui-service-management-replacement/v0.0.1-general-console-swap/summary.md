# 迭代摘要

- 目标：将侧边栏“中控台”调整为与“神经系统”一致的占位页，并把“设置 -> 通用”中的服务管理替换为原“中控台”的网关控制实现。
- 实现：新增可复用的 `GatewayControlPanel` 组件，承载网关状态、配置编辑、日志查看三块能力。
- 页面调整：`NormalMode.vue` 不再渲染“中控台”实际内容，统一显示“敬请期待”，并按要求加入禁止修改注释。
- 设置调整：`GeneralSettings.vue` 删除原服务安装/启动/停止逻辑，保留聊天偏好设置，并在下方嵌入网关控制面板。
- 前端清理：`desktop.ts` 移除已不再被前端使用的服务安装管理 API 导出，避免残留死代码。
