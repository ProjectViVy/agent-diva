请对 /c/Users/Administrator/Desktop/morediva/.workspace/loops/originals/openclaw 进行只读分析，提炼其 Loop Engineering（自动化循环工程）模式，为 agent-diva 下一阶段大型更新提供设计参考。

agent-diva 背景：Rust workspace，15 crate，核心模块包括 cron/heartbeat、spawn 子代理、sandbox 执行、laputa memory 等。

请重点回答：
1. 核心循环结构：触发（trigger）→ 计划（plan）→ 执行（execute）→ 验证（verify）→ 记忆（remember）→ 决策（decide）/重复。指出哪些阶段由代码/文档显式实现，哪些阶段隐含或缺失。
2. 覆盖 Loop Engineering 的哪些构件：automation、worktree、skills、connectors、sub-agents、memory、scheduler、watchdog、state machine、checkpoint、event/tap、publisher、mechanical gate、critic/integrator 等。
3. 与 agent-diva 现有架构（cron/heartbeat、spawn、sandbox、laputa memory）的映射/借鉴点。哪些可以直接复用/迁移，哪些需要适配。
4. 最值得 agent-diva 复用的 3 个具体设计或代码片段。每个必须给出：关键文件路径、代码/配置原文（约 30-80 行）、为什么值得复用。

分析范围提示：OpenClaw 仓库很大，请优先聚焦 docs/automation/（cron-jobs.md、cron-vs-heartbeat.md、taskflow.md、standing-orders.md、hooks.md、tasks.md、poll.md）、docs/agent-runtime-architecture.md、src/ 下与 agent runtime/scheduler/heartbeat/cron/task 相关的核心文件。不要遍历 apps/ui/ 等无关目录。

要求：
- 只读分析，不要修改任何代码，不要提交任何文件。
- 输出为中文 Markdown，保存到 /c/Users/Administrator/Desktop/morediva/agent-diva/docs/research/loop-engineering-references/openclaw-loop-analysis.md。
- 每条关键结论必须附带文件路径和原文摘录（代码或配置）。
- 报告控制在 1500-2500 字。
- 对无法确认的内容明确标注“未确认”。
