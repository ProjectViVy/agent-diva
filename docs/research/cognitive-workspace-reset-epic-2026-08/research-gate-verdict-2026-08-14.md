# Research Gate 评审记录（2026-08-14）

- 状态：`Partial Pass / R1 Held`
- 评审人：用户（对话，未开编辑器）
- 记录：本文件。产品形状仍以 8/14 决策与 [`docs/architecture/laputa/architecture.md`](../../architecture/laputa/architecture.md) 为准。

过 Gate = 研究可当 D0 输入。**不等于**改生产、不等于建保护分支、不等于 D3 可以抄 GA。

## 逐包

| 包 | 裁决 | 用户原意 |
| --- | --- | --- |
| R0 | **过** | 同意；按最新决策（七文件、P20、WORLD 工具等）读，不按包内「四份人格」旧措辞 |
| R1 | **挂起** | 提示词纪律要。建议方向对。但 **GA 自主进化到底怎么来的还没搞清**；搞清之前不做下一步 |
| R2 | **过** | OK；按最新决策（`ACTMEM.MD` 全局等） |
| R3 | **过** | 正确；奇奇怪怪的种子设计应删除；按最新决策 |
| R4 | **过（带备份政策）** | **无任何迁移，没了就没了。** 提交太乱、点不出旧 SHA。**文档收完后切一次分支备份**（不是现在，也不是 runtime fallback） |

## 明确留下的缺口（评审里没点名、或还不能当合同）

1. **整门还是分域。** R1 挂起时：D0 总图能否先画（Evolution 角标「未清」）？D1/D2 能否先设计？**默认：D3 与任何「落地 GA 进化」停；D0 若开，必须把 Evolution 标成 R1 未清，不得假装 Gate 全过。**
2. **R1c 因果页已写** [`ga-autonomy-origin.md`](../cognitive-r1-genericagent-evolution-2026-08/ga-autonomy-origin.md)。等用户看过再过 R1。
3. **保护分支：** 不追旧 SHA。文档收完后从当时 tip 切一次备份。现在不建。
4. **无任何迁移。** 旧数据删了就没了；不抽样、不手抄义务。
5. **活体 GA 若跑：** 用桌面 `keys.txt`（不入库）。R1c 机制说清优先于实验。
6. **ACTMEM 车道已冻（后补）：** 整份走工具，不自动装配；第一版一个读工具。

## 已接受的实施方向（仍不是开工令）

- 提示词纪律进入未来 Evolution 设计（等 R1 自主进化说清之后）。
- 删除 JSON/`null`/预种子 `# WORLD` 这类让「缺席」永远不成立的种子。
- 不做旧数据迁移/导入。没了就没了。
- 保护分支 = 文档收完后切备份分支，不是追历史 SHA，不是 runtime fallback。
- 活体实验密钥：桌面 `keys.txt`。
