# Discord C5 wire fixtures

These redacted fixtures describe the scripted Gateway and REST response
shapes consumed by the native adapter tests. The Gateway transcript covers
HELLO/IDENTIFY/READY, heartbeat ACK, reconnect and invalid-session opcodes;
REST data covers real snowflake-shaped IDs, rate limits and permission errors.
No credential, signed URL or private Discord identifier is stored here.
